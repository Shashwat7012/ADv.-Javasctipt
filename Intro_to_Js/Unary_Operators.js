// Postfix increment = ++a
// Postfix decriment = --a

// Prefix increment = a++
// Postfix decrement = a--

//typeof = unary operator

// One more operator :- unary plus(+) :- it converts into a number . if 
// it is not a number

let x = "22";
let y = +x;
//but we have to assing it first to another varible. it do not change the original but we can assign it another variable as a number.
console.log(y);

console.log(typeof(y));
