let name = "Shashwat";
let company = "Google";

// Interpolation using ` ` and dollar sign

let str1 = `${name} works in ${company}`;
console.log(str1);


let exp = `Interpolation example ${2+2-1}`;
console.log(exp);